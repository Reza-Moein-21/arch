# Enable dynamic opacity changes (required for transparency)
dynamic_background_opacity yes

# Set background opacity (0.0 = fully transparent, 1.0 = fully opaque)
background_opacity 0.85

# Enable background blur (requires a compositor)
background_blur 30
